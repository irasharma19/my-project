import { BackDetailsDirective } from './back-details.directive';

describe('BackDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new BackDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
